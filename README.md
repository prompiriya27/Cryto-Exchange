# Cryto-Exchange
This is a program written in Node.js that calls and receives commands from the backend and uses MongoDB to store the database, with its structure based on an ER Diagram. It handles processes starting from receiving user data, creating cryptocurrency wallets, placing buy/sell orders, and storing transaction history.
